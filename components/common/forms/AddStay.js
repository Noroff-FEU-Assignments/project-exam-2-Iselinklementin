import React, { useState } from "react";

function AddStay() {
  const [submitting, setSubmitting] = useState(false);
  const [serverError, setServerError] = useState(false);
  return <div>AddStay</div>;
}

export default AddStay;
